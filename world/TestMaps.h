#pragma once
#include <vector>
#include <string>

// Готовые тестовые карты в виде ASCII-рисунка: '#' — стена, любой другой символ
// (используем '.') — пол. Каждая строка — один ряд карты; все строки должны
// быть одной длины (см. Grid::Grid(const std::vector<std::string>&)).
//
// Это просто отдельные функции, а не файлы на диске — так проще держать
// несколько тестовых карт под рукой, пока не появится настоящий редактор/загрузка
// уровней из файла.
namespace TestMaps
{
    // Небольшая комната с "серверной стойкой" (полым 3x3 препятствием) в центре —
    // достаточно, чтобы проверить и рамку по периметру, и обход препятствия.
    inline std::vector<std::string> NeonAlley()
    {
        return {
            "###############",
            "#.............#",
            "#.............#",
            "#....###......#",
            "#....#.#......#",
            "#....###......#",
            "#..........#..#",
            "#.............#",
            "###############",
        };
    }
}
