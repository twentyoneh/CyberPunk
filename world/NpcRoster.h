#pragma once
#include "../game/Npc.h"
#include <vector>

// Готовые наборы NPC для сцен — тот же приём, что и в TestMaps.h: не плодить
// объявления отдельных NPC внутри main.cpp, а просто перечислить их данные
// здесь и передать одним вызовом в NpcManager. Чтобы добавить NPC — не нужно
// трогать main.cpp вообще, только дописать запись в нужный список ниже.
namespace NpcRoster
{
    inline std::vector<Npc> NeonAlleyNpcs()
    {
        return {
            {
                10, 2,
                "Панк с района",
                {
                    "Эй, чё как. Не видал тут корпо-патруль?",
                    "Если что — я ничего не говорил, понял?",
                    "Ладно, топай своей дорогой."
                }
            },
            // Ещё один NPC — просто ещё одна запись в этом списке, например:
            // {
            //     4, 6,
            //     "Торговец",
            //     { "Погляди товар, если найдёшь монеты." }
            // },
        };
    }
}
